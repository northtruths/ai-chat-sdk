7.24
LLMManager就是管理各种大模型的基类，是一个纯虚类/抽象基类

一个大语言模型，在开发角度可能拥有以下通用点
# 模型参数：
模型名字（ID）
发送的消息（这涉及挺多，有提示词（用户的、系统的、工具的之类），还有上下文的处理都属于这一模块）
除了以上两个必要参数，还有
api_key、endpoint
temperature
max_tokens
等核心设置参数，这些用可以一个结构封装起来


# 模型方法：
初始化模型
检测模型是否有效( url 和 key 是否有效)
发送信息（信息的发送方式：流式/全量式）

