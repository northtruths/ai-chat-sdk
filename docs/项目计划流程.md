Phase 1: 先跑通官方 API（验证可行性）
    ├── 用 apifox 调通 DeepSeek API
    ├── 用 apifox 调通 ChatGPT API
    └── 用 apifox 调通 Gemini API
         ↓
Phase 2: 手写 HTTP 客户端封装
    ├── 封装 httplib + OpenSSL
    ├── 实现 JSON 序列化/反序列化
    └── 实现 SSE 流式解析
         ↓
Phase 3: 实现各个 Provider
    ├── DeepSeekProvider（全量 + 流式）
    ├── ChatGPTProvider（全量 + 流式）
    ├── GeminiProvider（全量 + 流式）
    └── OllamaLLMProvider（本地接入）
         ↓
Phase 4: 组装成 ChatSDK
    ├── LLMManager（管理多个 Provider）
    ├── SessionManager（会话管理）
    ├── DataManager（SQLite 持久化）
    └── ChatSDK 统一对外接口
         ↓
Phase 5: 应用层（Web 服务器 + 前端）
    ├── HTTP 服务器（提供 RESTful API）
    └── 前端页面（聊天界面）